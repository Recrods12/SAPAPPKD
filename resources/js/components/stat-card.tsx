import type {LucideIcon} from 'lucide-react'; import {motion} from 'motion/react'; import {Card,CardContent} from '@/components/ui/card'; import {staggerItem} from '@/lib/motion';
export function StatCard({label,value,icon:Icon,tone='blue'}:{label:string;value:number|string;icon:LucideIcon;tone?:'blue'|'green'|'amber'|'red'}){const colors={blue:'bg-blue-50 text-blue-700',green:'bg-emerald-50 text-emerald-700',amber:'bg-amber-50 text-amber-700',red:'bg-red-50 text-red-700'};return <motion.div variants={staggerItem}><Card><CardContent className="flex items-start justify-between"><div><p className="text-sm text-muted-foreground">{label}</p><strong className="mt-2 block text-3xl font-bold">{value}</strong></div><span className={`grid size-11 place-items-center rounded-xl ${colors[tone]}`}><Icon className="size-5"/></span></CardContent></Card></motion.div>}
