import type{ReactNode}from"react";import{Link,router,usePage}from"@inertiajs/react";import{Bell,LayoutDashboard,LogOut}from"lucide-react";import{Brand}from"@/components/brand";import{Button}from"@/components/ui/button";import type{SharedProps}from"@/types";
export function InstructorLayout({children,title}:{children:ReactNode;title:string}){const{auth}=usePage<SharedProps>().props;return <div className="min-h-screen bg-slate-50"><header className="border-b bg-slate-950 text-white"><div className="mx-auto flex max-w-6xl items-center justify-between p-4"><Link href={route("instructor.dashboard")}><Brand inverse/></Link><nav className="flex items-center gap-2"><Button asChild variant="ghost" className="text-white"><Link href={route("instructor.dashboard")}><LayoutDashboard/>Dashboard</Link></Button><Button asChild variant="ghost" className="relative text-white"><Link href={route("notifications.index")}><Bell/>{auth.user?.unread_notifications_count? <span className="absolute right-0 top-0 rounded-full bg-red-500 px-1 text-[10px]">{auth.user.unread_notifications_count}</span>:null}</Link></Button><Button variant="ghost" className="text-white" onClick={()=>router.post(route("logout"))}><LogOut/></Button></nav></div></header><main className="mx-auto max-w-6xl p-4 sm:p-6"><p className="text-xs font-semibold tracking-widest text-primary">PANEL INSTRUKTUR</p><h1 className="mt-1 text-2xl font-bold">{title}</h1><div className="mt-6">{children}</div></main></div>}
