import{Head,Link}from"@inertiajs/react";import{AlertTriangle,School,UserCheck,UsersRound}from"lucide-react";import{InstructorLayout}from"@/layouts/instructor-layout";import{StatCard}from"@/components/stat-card";import{Button}from"@/components/ui/button";
interface Class{id:number;name:string;room:string|null;enrollments_count:number;training_batch:{name:string;training_program:{name:string}}}export default function Dashboard({classes,stats}:{classes:Class[];stats:{classes:number;participants:number;presentToday:number;needsReview:number}}){return <InstructorLayout title="Ringkasan kelas"><Head title="Dashboard Instruktur"/><div className="grid gap-3 sm:grid-cols-4"><StatCard label="Kelas" value={stats.classes} icon={School}/><StatCard label="Peserta" value={stats.participants} icon={UsersRound}/><StatCard label="Hadir hari ini" value={stats.presentToday} icon={UserCheck} tone="green"/><StatCard label="Perlu ditinjau" value={stats.needsReview} icon={AlertTriangle} tone="red"/></div><section className="mt-6 grid gap-3 sm:grid-cols-2"><h2 className="sm:col-span-2 text-lg font-bold">Kelas yang ditugaskan</h2>{classes.map(item=><article key={item.id} className="rounded-2xl border bg-white p-5"><p className="text-sm text-primary">{item.training_batch.training_program.name} · {item.training_batch.name}</p><h3 className="mt-1 text-lg font-bold">{item.name}</h3><p className="mt-2 text-sm text-muted-foreground">{item.enrollments_count} peserta · Ruang {item.room||"-"}</p><Button asChild className="mt-4" variant="outline"><Link href={route("instructor.classes.show",item.id)}>Lihat peserta</Link></Button></article>)}{classes.length===0&&<div className="sm:col-span-2 rounded-xl border bg-white p-10 text-center text-muted-foreground">Belum ada kelas yang ditugaskan.</div>}</section></InstructorLayout>}
