import {Head,Link} from "@inertiajs/react";import {ArrowLeft,CalendarDays,Clock3,MapPin} from "lucide-react";import {Button} from "@/components/ui/button";
interface Schedule{id:number;schedule_date:string;start_time:string;end_time:string;subject:string|null;attendance_location:{name:string;address:string}|null}export default function Schedule({schedules}:{schedules:{data:Schedule[]}|null}){return <main className="min-h-screen bg-slate-50"><Head title="Jadwal Pelatihan"/><header className="bg-slate-950 p-5 text-white"><div className="mx-auto max-w-3xl"><Button asChild variant="ghost" className="-ml-3 text-white"><Link href={route("dashboard")}><ArrowLeft/>Kembali</Link></Button><h1 className="mt-3 text-2xl font-bold">Jadwal pelatihan</h1></div></header><section className="mx-auto grid max-w-3xl gap-3 p-4">{schedules?.data.map(item=><article key={item.id} className="rounded-2xl border bg-white p-5"><p className="flex items-center gap-2 text-sm text-primary"><CalendarDays className="size-4"/>{new Date(item.schedule_date).toLocaleDateString("id-ID",{weekday:"long",day:"numeric",month:"long",year:"numeric"})}</p><h2 className="mt-2 font-bold">{item.subject||"Pelatihan"}</h2><p className="mt-3 flex gap-2 text-sm"><Clock3 className="size-4"/>{item.start_time}–{item.end_time}</p>{item.attendance_location&&<p className="mt-2 flex gap-2 text-sm text-muted-foreground"><MapPin className="size-4"/>{item.attendance_location.name}</p>}</article>)}{(!schedules||schedules.data.length===0)&&<div className="rounded-2xl border bg-white p-10 text-center text-muted-foreground">Belum ada jadwal pelatihan.</div>}</section></main>}
